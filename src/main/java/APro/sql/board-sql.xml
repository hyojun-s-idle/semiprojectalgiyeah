<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<comment>board-sql.xml</comment>
	<entry key="test">test</entry>
	
	<!-- 게시글 이름 및 카테고리 이름 -->
	<entry key="selectBoardName">
	SELECT BOARD_NAME 
	FROM BOARD_TYPE
	WHERE BOARD_TYPE_CD = ?
	</entry>
	
	<!-- 카테고리 게시글 수 -->
	<entry key="getListCountCate">
	SELECT COUNT(*) 
	FROM BOARD
	WHERE BOARD_TYPE_CD = ?
	</entry>
	
	<!-- 카테고리 전체 가져오기 -->
	<entry key="selectCategoryAll">
	SELECT * FROM BOARD_TYPE
	</entry>
	
	<!-- 게시판 게시글 전체 조회 -->
	<entry key="getListCount">
	SELECT COUNT(*) 
	FROM BOARD
	JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	WHERE PR_BOARD = ?
	</entry>
	
	<!-- 카테고리 게시물 목록 조회 -->
	<entry key="selectCategoryList">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE BOARD_TYPE_CD = ?
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<!-- 게시판 전체 게시물 조회 목록 -->
	<entry key="selectBoardList">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE PR_BOARD = ?
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<!-- 카테고리 제목 검색 게시물 수 -->
	<entry key="searchTitle">
	SELECT COUNT(*) FROM BOARD
	WHERE BOARD_TYPE_CD = ?
	AND BOARD_TITLE LIKE '%' || ? ||'%'
	</entry>
	
	<!-- 제목 검색 게시물 수 -->
	<entry key="searchTitleAll">
	SELECT COUNT(*) FROM BOARD
	WHERE BOARD_TITLE LIKE '%' || ? ||'%'
	</entry>
	
	<!-- 카테고리 작성자 검색 게시물 수 -->
	<entry key="searchWriter">
	SELECT COUNT(*) FROM BOARD
	JOIN MEMBER USING(MEMBER_NO)
	WHERE BOARD_TYPE_CD = ?
	AND MEMBER_NICK LIKE '%' || ? || '%'
	</entry>
	
	<!-- 작성자 검색 게시물 수 -->
	<entry key="searchWriterAll">
	SELECT COUNT(*) FROM BOARD
	JOIN MEMBER USING(MEMBER_NO)
	WHERE MEMBER_NICK LIKE '%' || ? || '%'
	</entry>
	
	<!-- 카테고리 내용 검색 게시물 수 -->
	<entry key="searchContent">
	SELECT COUNT(*) FROM BOARD
	WHERE BOARD_TYPE_CD = ?
	AND BOARD_CONTENT LIKE '%' || ? ||'%'
	</entry>
	
	<!-- 내용 검색 게시물 수 -->
	<entry key="searchContentAll">
	SELECT COUNT(*) FROM BOARD
	WHERE BOARD_CONTENT LIKE '%' || ? ||'%'
	</entry>
	
	<!-- 카테고리 제목 검색 게시물 목록 조회 -->
	<entry key="searchListTitle">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE BOARD_TYPE_CD = ?
	    AND BOARD_TITLE LIKE '%' || ? ||'%'
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<!-- 제목 검색 게시물 목록 조회 -->
	<entry key="searchListTitleAll">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	     BOARD_TITLE LIKE '%' || ? ||'%'
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<!-- 카테고리 작성자 검색 게시물 목록 조회 -->
	<entry key="searchListWriter">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE BOARD_TYPE_CD = ?
	    AND MEMBER_NICK LIKE '%' || ? ||'%'
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<!-- 작성자 검색 게시물 목록 조회 -->
	<entry key="searchListWriterAll">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE MEMBER_NICK LIKE '%' || ? ||'%'
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<!-- 카테고리 내용 검색 게시물 목록 조회 -->
	<entry key="searchListCon">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE BOARD_TYPE_CD = ?
	    AND BOARD_CONTENT LIKE '%' || ? ||'%'
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<!-- 내용 검색 게시물 목록 조회 -->
	<entry key="searchListConAll">
	SELECT * 
	FROM (SELECT ROWNUM RNUM, A.*
	FROM (SELECT BOARD_NO,BOARD_TITLE,BOARD_CONTENT,
	        TO_CHAR(CREATE_DT,'YYYY.MM.DD') AS "CREATE_DT"
	        ,PROFILE_IMGE,MEMBER_NICK,BOARD_NAME
	    FROM BOARD
	    JOIN BOARD_TYPE USING(BOARD_TYPE_CD)
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE BOARD_CONTENT LIKE '%' || ? ||'%'
	    ORDER BY BOARD_NO DESC)A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
</properties>
